- what is the input to the program?
- what is the output of the program?
- what is the input to each recursive call?
- what is the output of each recursive call?

1. input: string
2. output: arrays
3. func(string, separator)
4. arrays
